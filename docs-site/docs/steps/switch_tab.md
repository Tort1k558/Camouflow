# switch_tab

Переключается на вкладку по индексу.

## Параметры

- `tab_index` / `index` *(int, optional)* — индекс вкладки (0..N-1).
- `from_var` *(string, optional)* — взять индекс из переменной.

## Пример

```json
{ "action": "switch_tab", "tag": "Tab2", "tab_index": 1 }
```

